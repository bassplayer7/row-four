import React from "react";
import './BoardCell.css'

export default function BoardCell({x, y, key}) {

    return (
        <div className={'BoardCell'}
             key={key}>

        </div>
    )
}
